//! High-level actions on [`LiFiClient`](crate::LiFiClient).
//!
//! These are convenience orchestration methods that route queries to
//! the appropriate chain [`Provider`](crate::provider::Provider).

mod balance;
mod name_service;
